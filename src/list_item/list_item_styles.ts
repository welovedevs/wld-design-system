import createStyles from '@material-ui/styles/createStyles';

export const styles = createStyles({
    container: {
        padding: '15px 20px',
        borderRadius: 5,
        display: 'flex',
        alignItems: 'center',
    },
    button: {
        cursor: 'pointer',
    },
    typography: {
        display: 'flex',
        alignItems: 'center',
    },
});
