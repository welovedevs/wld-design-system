import createStyles from "@mui/styles/createStyles";

export const styles = createStyles({
    container: {
        width: '100%',
        padding: 24,
        fontSize: 20,
        lineHeight: 1.6,
        fontWeight: 500,
        letterSpacing: 'unset',
    },
});
