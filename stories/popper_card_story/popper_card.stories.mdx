import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { PopperCardStory } from './popper_card_story';
import { PopperCard } from '../../src';

<Meta title="Components/PopperCard" component={PopperCard} />

# Popper

<Canvas>
    <Story name="Default">
        <div className="ds-flex ds-items-center ds-flex-wrap">
            <PopperCardStory />
            <PopperCardStory popperProps={{ placement: 'bottom' }} />
            <PopperCardStory popperProps={{ placement: 'left' }} />
            <PopperCardStory popperProps={{ placement: 'right' }} />
            <PopperCardStory popperProps={{ placement: 'top' }} />
        </div>
    </Story>   <Story name="Structured">
        <div className="ds-flex ds-items-center ds-flex-wrap">
            <PopperCardStory structured />
        </div>
    </Story>
</Canvas>
