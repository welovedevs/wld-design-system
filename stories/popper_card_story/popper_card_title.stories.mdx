import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { PopperCardTitle } from '../../src/popper_card_title/popper_card_title';
import { Button } from '../../src/button/button';

<Meta title="Components/PopperCard" component={PopperCardTitle} />

export const Template = (args) => {
    return (
        <PopperCardTitle>
          Coucou
        </PopperCardTitle>
    );
};

# PopperCardTitle

<Canvas>
    <Story name="Title">{Template.bind({})}</Story>
</Canvas>
